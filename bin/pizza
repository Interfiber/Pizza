#!/bin/bash

if [[ $1 == "" ]]; then
	echo "Nothing to do. No pizza file was dictated"
fi
if [[ $1 == "compile" ]]; then
	java -jar $PIZZA_HOME/build/libs/Pizza-0.1\ Beta.jar $2
	rm -rf $TMPDIR/pizza
fi
